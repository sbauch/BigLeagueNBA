<%= form_for(@player_ownership) do |f| %>
  <% if @player_ownership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_ownership.errors.count, "error") %> prohibited this player_ownership from being saved:</h2>

      <ul>
      <% @player_ownership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
